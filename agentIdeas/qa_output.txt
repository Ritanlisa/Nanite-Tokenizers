根据SysML知识图谱数据，获取R1P3机柜所有节点加电信息的步骤如下：

1. **通过mn0节点执行SMU命令转发**
   在mn0节点上分别对R1P3机柜的4个子机柜（a/b/c/d）执行以下命令：
   ```
   smu_tranfer_cmd r1.p03a.m yhst
   smu_tranfer_cmd r1.p03b.m yhst
   smu_tranfer_cmd r1.p03c.m yhst
   smu_tranfer_cmd r1.p03d.m yhst
   ```
   - `r1.p03a.m`表示R1列P3机柜的a子机柜CMU（文档1.4节说明机柜编号规则）
   - `yhst`是文档6.3节定义的"查看所有结点加电信息"命令，通过SMU转发可安全获取状态

2. **命令依据**
   - `yhst`命令的`invocation`字段明确指向`smu_tranfer_cmd`执行方式
   - R1P3机柜对应文档中的"R1P0-R1P3号柜"，其子机柜编号为a/b/c/d（R1P3实体描述）

## QA 反馈
1. **缺失命令执行权限验证**：未说明mn0节点是否有权限执行`smu_tranfer_cmd`命令
2. **缺少子机柜编号确认**：未明确R1P3机柜的子机柜编号是否确实为a/b/c/d（需文档1.4节佐证）
3. **未提及错误处理**：未说明命令执行失败时的排查方法（如网络中断、CMU离线等场景）