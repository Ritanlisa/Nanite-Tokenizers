# Update AGENTS.md

更新项目知识文件，反映 2026-05 以来的所有变更。

## TODOs

- [x] 1. 更新 AGENTS.md — 补充 20-type 关系系统、BFS cascade 算法、radial tree 布局、build.json 续跑、Ograg2 对比、当前状态
  - Before: `sysml/sysml_model.py` 读取关系类型定义
  - Before: `web/kg_viz.html:277-382` 读取布局代码
  - Before: `agent/kg_build_agent.py` 读取 cascade 逻辑
  - Before: `scripts/build_kg_recursive.py` 读取构建配置
  - Write: `/media/ritanlisa/Weights/Nanite-Tokenizers/AGENTS.md`

## Success Criteria
- `cat AGENTS.md | grep "20-Type SysML"` matches
- `cat AGENTS.md | grep "BFS Cascade"` matches
- `cat AGENTS.md | grep "Radial tree"` matches
- `cat AGENTS.md | grep "Ograg2"` matches
- `cat AGENTS.md | grep "当前状态"` matches

## Dependencies
- None (read-only research)
